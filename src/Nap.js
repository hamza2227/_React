import React from 'react';

const Nap = (props) => {
    return (
        <div>
            {/* <h5>Kids are {props.name}</h5>
            <h5>Kids are {props.play}</h5> */}
            <h4>Value of counter is: {props.counter}</h4>
        </div>
    )
}

export default Nap