import React from 'react';
import Child from './Child';

const Parent = () => {
    return (
        <div>
            <h4>Parent</h4>
            <Child />
        </div>
    )
}

export default Parent